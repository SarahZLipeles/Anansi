"use strict";
var expect = require("chai").expect;
var path = require("path");
var dir = require("../../game.paths");
var buildUserScope = require(path.join(dir.builders, "userScope"));

// describe("UserScope", () => {
// 	var scope;
// 	beforeEach(() => {
// 		scope = buildUserScope({

// 		});
// 	})
// });